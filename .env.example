# Frontend environment variables (Vite)
# Copy this file to .env and fill in the values.
#
# LOCAL DEVELOPMENT: leave VITE_API_URL empty — Vite proxies /api/* to localhost:3001
# PRODUCTION (Vercel): set VITE_API_URL to your Render backend URL (no trailing slash)

VITE_API_URL=
